# Secrets / environment
.env

# Python
__pycache__/
*.pyc
*.pyo

# Vector store (rebuilt from research/ via reindex())
memory/.chroma/

# Bulk data artifacts (keep the folder via .gitkeep)
data/*
!data/.gitkeep

# Scratch exec
scratch/_exec.py

# Loop runtime state (human-in-the-loop escalation)
.notified
.all-blocked
NEEDS_HUMAN.log
